{
  "name": "soenglish",
  "private": true,
  "description": "SoEnglish — API + Next клієнт",
  "scripts": {
    "setup": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -k -n api,web -c blue,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "lint": "npm run lint --prefix client",
    "test:unit": "npm run test:unit --prefix server",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
