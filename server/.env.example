PORT=3000

# Запуск API або seed на своїй машині (поза Docker):
MONGO_URI=mongodb://127.0.0.1:27017/soenglish

# Усередині Docker Compose хост Mongo — `mongo` (це прописано в docker-compose.yml, .env може не читатися контейнером).
